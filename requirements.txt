# --- Núcleo (necessário para rodar a demo local, sem Azure) ---
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
python-dotenv>=1.0

# --- Vídeo: postura (substitui OpenPose) e detecção de objetos ---
opencv-python-headless>=4.9
## mediapipe can be heavy and some environments (Python 3.13) may not
## have a wheel for the exact pinned version. Make it optional so the
## demo and training that rely on synthetic data can run without it.
# If you need the MediaPipe pose/video features, install one of these:
# pip install mediapipe==0.10.9    # or a newer compatible mediapipe for your Python

ultralytics>=8.2          # YOLOv8

# --- Áudio: extração de características acústicas ---
librosa>=0.10
soundfile>=0.12

# --- Áudio: eventos sonoros via YAMNet, treinado no AudioSet de verdade ---
# (opcional — o pipeline funciona sem isso, só pula essa etapa)
tensorflow>=2.16
tensorflow-hub>=0.16

# --- Dados médicos reais (PhysioNet / VitalDB) ---
wfdb>=4.1
vitaldb>=1.4

# --- Vídeos reais de fisioterapia (dataset Kaggle, opcional) ---
kagglehub>=0.3

# --- Azure Cognitive Services ---
azure-cognitiveservices-speech>=1.38
azure-ai-textanalytics>=5.3

# --- OpenAI (parecer final consolidado por LLM) ---
openai>=1.40

# --- Treinamento de modelos e persistência ---
joblib>=1.3
matplotlib>=3.8

# --- Frontend de demonstração ---
streamlit>=1.35
plotly>=5.20

# --- Testes ---
pytest>=8.0
